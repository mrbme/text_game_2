---
title: Orion 3033 — retro terminal rescue game
status: draft
---

### Overview

Orion 3033 is a type-only retro game that runs as a single page in this Next.js 16 app. The player is the captain of the mining ship O.S.S. ORION, guiding the stranded crew of the survey ship CASSIUS out of a cave system on LV-221 by text relay — visual comms are down, so every reply the player types is a message the crew acts on, while something hunts them in the dark.

The canvas above shows every screen: attract-mode title with animated starfield, ominous intro crawl, ship-OS boot sequence, and the terminal where the whole game plays out, plus its help, high-threat, fail, and win states. A standalone homage prototype of the boot screen lives at `plans/orion-3033/boot-demo.html`; its line script becomes `lib/game/boot-script.ts`.

**Done means:** `npm run dev` serves the complete loop — title → intro → boot → terminal — with a full authored campaign (3 acts, ~15 story nodes, 15–20 minutes) playable start to finish by typing; both endings reachable; runs are ephemeral — nothing persists and losing starts a fresh run; fully keyboard-playable; `npm run build` and `npm run lint` clean.

**Non-goals for this cut:** no backend, accounts, or route handlers; no persistence of any kind (settled below); desktop-first layout (mobile works but is not optimized); no LLM-driven dialogue (settled in Approach below); no i18n. Audio is the one genuinely open call — see Open Questions at the bottom.

### Approach

The game is a small phase machine wrapping a data-driven story graph. All state lives in one `GameState` object driven by a pure reducer, so the UI components stay thin and the story is authored as data, not code.

<Callout id="decision-client-only" tone="decision">

**Pure client SPA, zero new runtime dependencies.** `app/page.tsx` renders a single `'use client'` `<Game />` component. No server actions, no API routes, no game-engine or animation library — and **no three.js**: the aesthetic is flat 8-bit, so the starfield, scanlines, flicker, and glow are pure CSS, and everything else is text. A WebGL scene would add hundreds of KB to fight the pixel look. The only additions are two Google pixel fonts self-hosted via `next/font/google`. Story text ships in the bundle (~15 KB).

</Callout>

<Callout id="decision-parser" tone="decision">

**Deterministic keyword parser, not NLP.** Each story node declares keyword groups (`['left', 'west']`) that map to a reply and a next node. Global commands (HELP, STATUS, LOOK) are checked first. Unmatched input gets an in-fiction fallback ("say again? the signal is chewing your words"), so the game never breaks character. This keeps the campaign authorable, testable, and fully offline. If LLM-flavored dialogue ever becomes worth it, it would replace only fallback lines behind the same parser — no engine changes — but it is out of scope here.

</Callout>

<Callout id="decision-ephemeral" tone="decision">

**Ephemeral runs: you either win or lose.** No localStorage, no CONTINUE, no mid-run checkpoints. A run ends rescued or lost; SIGNAL LOST returns to the title and the next run starts clean. The mitigations for losing a run are built into the design: the campaign is 15–20 minutes, the threat meter telegraphs danger before it maxes, and the overrun node offers one last-ditch escape before the lost ending.

</Callout>

### Phase machine

<Diagram id="phase-diagram" caption="Four phases; the terminal phase loops one reducer pass per typed command.">

```html
<div class="diagram-panel" style="display:flex;flex-direction:column;gap:16px">
  <div style="display:flex;align-items:center;gap:10px;flex-wrap:wrap">
    <div class="diagram-node">TITLE</div>
    <span class="diagram-muted">START →</span>
    <div class="diagram-node">INTRO CRAWL</div>
    <span class="diagram-muted">ENTER →</span>
    <div class="diagram-node">OS BOOT</div>
    <span class="diagram-muted">auto →</span>
    <div class="diagram-node">TERMINAL</div>
  </div>
  <div class="diagram-box" style="display:flex;flex-direction:column;gap:10px;padding:14px">
    <span class="diagram-muted">inside TERMINAL — one loop per typed command</span>
    <div style="display:flex;align-items:center;gap:10px;flex-wrap:wrap">
      <div class="diagram-card">input line</div>
      <span class="diagram-muted">→</span>
      <div class="diagram-card">parser: ending node? global cmds? node keywords?</div>
      <span class="diagram-muted">→</span>
      <div class="diagram-card">engine reducer (pure)</div>
      <span class="diagram-muted">→</span>
      <div class="diagram-card">next node + new log lines</div>
    </div>
    <div style="display:flex;gap:10px;flex-wrap:wrap">
      <div class="diagram-pill">ending: rescued → new game</div>
      <div class="diagram-pill">ending: lost → run over, back to title</div>
    </div>
  </div>
</div>
```

</Diagram>

### Story engine and data model

A story node is one cave chamber or story beat: the crew lines that arrive when it is entered, a `look` description for the global LOOK command, a `label` for the status bar, and the keyword matches that leave it. The reducer echoes the player line, short-circuits on ending nodes (rescued or lost, any input starts a fresh run — the Terminal also maps bare Enter on an ending node to the restart action), then tries global commands, then node keywords, applies any threat delta, and enters the next node.

Threat is a 0–5 meter — noisy choices raise it, stealth choices lower it, and at 5 the engine jumps to the overrun node regardless of the route picked. Stats have explicit producers: `wrongTurns` increments on any match with a positive `threatDelta`, `closeCalls` on each threat overflow, elapsed time derives from `startedAt` (stamped when the terminal opens), and crew count is the constant 3.

A graph-validation `bun test` asserts that every `match.next`, `START_ID`, and `OVERRUN_ID` resolve to real nodes and that both endings are reachable — a typo in act data fails the suite, not minute nineteen of a playthrough.

<TabsBlock
  id="key-files"
  orientation="vertical"
  tabs={[
    {
      id: "tab-types",
      label: "lib/game/types.ts",
      blocks: [
        {
          id: "code-types",
          type: "annotated-code",
          data: {
            filename: "lib/game/types.ts",
            language: "ts",
            code: "export type Phase = 'title' | 'intro' | 'boot' | 'terminal'\n\nexport type GameAction =\n  | { type: 'advance'; now?: number }\n  | { type: 'command'; input: string }\n  | { type: 'restart' }\n\nexport interface TerminalLine {\n  id: number\n  from: 'system' | 'crew' | 'player'\n  text: string\n}\n\nexport interface CommandMatch {\n  keywords: string[]\n  reply: string\n  next: string\n  threatDelta?: number\n}\n\nexport interface StoryNode {\n  id: string\n  label: string\n  look: string\n  entryLines: string[]\n  matches: CommandMatch[]\n  fallback: string\n  ending?: 'rescued' | 'lost'\n}\n\nexport interface GameState {\n  phase: Phase\n  nodeId: string\n  threat: number\n  lines: TerminalLine[]\n  nextId: number\n  wrongTurns: number\n  closeCalls: number\n  startedAt: number\n}",
            annotations: [
              { lines: "3-6", label: "Action contract", note: "Everything the UI can dispatch. game.tsx and terminal.tsx never touch GameState directly — only these three actions." },
              { lines: "20-28", label: "Story beat", note: "label feeds the status bar LOC pill; look serves the global LOOK command. Both exist before content is authored so 15 nodes never need a schema migration. Ending nodes terminate the run." },
              { lines: "14-18", label: "Route edge", note: "Keyword group to reply and next node. threatDelta is the stealth or noise cost of the choice." },
              { lines: "30-39", note: "Single immutable state object — no persistence anywhere. wrongTurns, closeCalls, and startedAt produce the end-of-run stats; crew count is the constant 3." }
            ]
          }
        }
      ]
    },
    {
      id: "tab-engine",
      label: "lib/game/engine.ts",
      blocks: [
        {
          id: "code-engine",
          type: "annotated-code",
          data: {
            filename: "lib/game/engine.ts",
            language: "ts",
            code: "import { findNode, OVERRUN_ID, START_ID } from './story'\nimport { matchNode, globalReply } from './parser'\nimport type { GameAction, GameState, Phase, StoryNode, TerminalLine } from './types'\n\nconst append = (s: GameState, from: TerminalLine['from'], text: string): GameState => ({\n  ...s,\n  lines: [...s.lines, { id: s.nextId, from, text }],\n  nextId: s.nextId + 1,\n})\n\nconst enterNode = (s: GameState, node: StoryNode): GameState =>\n  node.entryLines.reduce(\n    (acc, line) => append(acc, 'crew', line),\n    { ...s, nodeId: node.id },\n  )\n\nconst bumpThreat = (s: GameState, delta = 0): GameState => {\n  const threat = Math.min(5, Math.max(0, s.threat + delta))\n  if (threat < 5) return { ...s, threat }\n  return enterNode({ ...s, threat: 3, closeCalls: s.closeCalls + 1 }, findNode(OVERRUN_ID))\n}\n\nconst applyCommand = (s: GameState, raw: string): GameState => {\n  const input = raw.trim().toLowerCase()\n  if (input === '') return s\n  const echoed = append(s, 'player', raw.trim())\n  const node = findNode(echoed.nodeId)\n  if (node.ending) return initialState\n  const global = globalReply(echoed, input)\n  if (global) return global\n  const match = matchNode(node, input)\n  if (!match) return append(echoed, 'crew', node.fallback)\n  const wrongTurns = (match.threatDelta ?? 0) > 0 ? echoed.wrongTurns + 1 : echoed.wrongTurns\n  const replied = append({ ...echoed, wrongTurns }, 'crew', match.reply)\n  const after = bumpThreat(replied, match.threatDelta)\n  return after.nodeId !== replied.nodeId ? after : enterNode(after, findNode(match.next))\n}\n\nconst PHASE_ORDER: Phase[] = ['title', 'intro', 'boot', 'terminal']\n\nexport const initialState: GameState = {\n  phase: 'title', nodeId: START_ID, threat: 0,\n  lines: [], nextId: 0, wrongTurns: 0, closeCalls: 0, startedAt: 0,\n}\n\nexport const gameReducer = (s: GameState, action: GameAction): GameState => {\n  if (action.type === 'command') return applyCommand(s, action.input)\n  if (action.type === 'restart') return initialState\n  const phase = PHASE_ORDER[Math.min(PHASE_ORDER.indexOf(s.phase) + 1, 3)]\n  if (phase !== 'terminal') return { ...s, phase }\n  return enterNode({ ...s, phase, startedAt: action.now ?? 0 }, findNode(START_ID))\n}",
            annotations: [
              { lines: "23-38", label: "One command, one pass", note: "Echo the player line, short-circuit ending nodes, try global commands, then node keywords; the fallback keeps the fiction when nothing matches. wrongTurns counts noisy choices." },
              { lines: "28", label: "Ephemeral endings", note: "On a rescued or lost node, any typed input returns a fresh initialState — the whole run evaporates. Terminal also maps bare Enter here to the restart action." },
              { lines: "36-37", label: "Overflow detection", note: "bumpThreat jumping to overrun CHANGES nodeId, so comparing against replied.nodeId detects the jump — and a player already standing in overrun can still escape via match.next. Unit-tested in the engine stage." },
              { lines: "47-53", label: "Reducer contract", note: "The exact function game.tsx wires into useReducer; advance carries a timestamp so the elapsed stat has a producer." }
            ]
          }
        }
      ]
    },
    {
      id: "tab-story",
      label: "lib/game/story/act1.ts",
      blocks: [
        {
          id: "code-story",
          type: "annotated-code",
          data: {
            filename: "lib/game/story/act1.ts",
            language: "ts",
            code: "import type { StoryNode } from '../types'\n\nexport const act1: StoryNode[] = [\n  {\n    id: 'crash-site',\n    label: 'CRASH SITE',\n    look: 'HALLORAN: a tilted cargo bay, half buried. storm howling outside. two passages — one level, one sloping down.',\n    entryLines: [\n      'HALLORAN: we are down. ship split on impact. we crawled into the cave to escape the storm.',\n      'HALLORAN: emergency lamp is dying. two passages ahead. one slopes down, one stays level.',\n    ],\n    matches: [\n      { keywords: ['level', 'straight', 'ahead'], reply: 'HALLORAN: ok. taking the level passage. the floor is wet here.', next: 'river-gallery' },\n      { keywords: ['down', 'slope', 'lower'], reply: 'HALLORAN: heading down. it is warmer this way. that smell…', next: 'nest-approach', threatDelta: 1 },\n    ],\n    fallback: 'HALLORAN: say again? the signal is chewing your words. the level passage, or the slope?',\n  },\n]",
            annotations: [
              { lines: "12-15", label: "Authoring format", note: "Routes are plain data — adding a chamber is adding an object, no engine changes." },
              { lines: "6-7", label: "label + look", note: "label shows in the status bar LOC pill; look answers the global LOOK command in this chamber." },
              { lines: "16", note: "Fallbacks restate the choice in-fiction so an unmatched reply never dead-ends the player." }
            ]
          }
        }
      ]
    },
    {
      id: "tab-hook",
      label: "hooks/use-typewriter.ts",
      blocks: [
        {
          id: "code-hook",
          type: "annotated-code",
          data: {
            filename: "hooks/use-typewriter.ts",
            language: "ts",
            code: "'use client'\n\nimport { useEffect, useState } from 'react'\n\nexport const useTypewriter = (text: string, speedMs = 24) => {\n  const [count, setCount] = useState(0)\n  const done = count >= text.length\n  useEffect(() => {\n    setCount(0)\n  }, [text])\n  useEffect(() => {\n    if (done) return\n    const id = setInterval(() => setCount((c) => c + 1), speedMs)\n    return () => clearInterval(id)\n  }, [done, text, speedMs])\n  return { visible: text.slice(0, count), done, skip: () => setCount(text.length) }\n}",
            annotations: [
              { lines: "11-15", label: "Self-clearing", note: "The interval tears down when done flips — no timer runs while text is fully revealed." },
              { lines: "16", note: "skip() powers Enter-to-reveal on the intro, boot, and terminal replies." }
            ]
          }
        }
      ]
    },
    {
      id: "tab-game",
      label: "components/game/game.tsx",
      blocks: [
        {
          id: "code-game",
          type: "annotated-code",
          data: {
            filename: "components/game/game.tsx",
            language: "tsx",
            code: "'use client'\n\nimport { useReducer } from 'react'\nimport { TitleScreen } from './title-screen'\nimport { IntroCrawl } from './intro-crawl'\nimport { BootSequence } from './boot-sequence'\nimport { Terminal } from './terminal'\nimport { gameReducer, initialState } from '@/lib/game'\n\nexport const Game = () => {\n  const [state, dispatch] = useReducer(gameReducer, initialState)\n  if (state.phase === 'title') return <TitleScreen onStart={() => dispatch({ type: 'advance' })} />\n  if (state.phase === 'intro') return <IntroCrawl onDone={() => dispatch({ type: 'advance' })} />\n  if (state.phase === 'boot') return <BootSequence onDone={() => dispatch({ type: 'advance', now: Date.now() })} />\n  return <Terminal state={state} dispatch={dispatch} />\n}",
            annotations: [
              { lines: "10-16", label: "Phase router", note: "The single client boundary. app/page.tsx stays a one-line server component that renders <Game />. The boot handler stamps startedAt for the elapsed stat." }
            ]
          }
        }
      ]
    }
  ]}
/>

### Files

<FileTree
  id="file-map"
  title="Files touched"
  entries={[
    { path: "app/layout.tsx", change: "modified", note: "Swap Geist fonts for Press Start 2P + VT323; game metadata" },
    { path: "app/page.tsx", change: "modified", note: "Boilerplate replaced with <Game />" },
    { path: "app/globals.css", change: "modified", note: "CRT tokens, scanlines, glow, starfield keyframes, reduced-motion guards" },
    { path: "components/game/game.tsx", change: "added", note: "Phase router over gameReducer (client entry)" },
    { path: "components/game/title-screen.tsx", change: "added", note: "Attract mode — every visit starts fresh" },
    { path: "components/game/intro-crawl.tsx", change: "added" },
    { path: "components/game/boot-sequence.tsx", change: "added", note: "Ships the boot-demo.html homage as React + boot-script data" },
    { path: "components/game/terminal.tsx", change: "added", note: "Log + input + status bar" },
    { path: "components/game/starfield.tsx", change: "added", note: "CSS star layers, shared by title screen" },
    { path: "components/game/index.ts", change: "added", note: "Barrel" },
    { path: "lib/game/types.ts", change: "added", note: "Phase, GameAction, StoryNode, GameState" },
    { path: "lib/game/engine.ts", change: "added", note: "gameReducer, applyCommand, initialState — all pure" },
    { path: "lib/game/parser.ts", change: "added", note: "Keyword matching + HELP / STATUS / LOOK" },
    { path: "lib/game/boot-script.ts", change: "added", note: "Boot sequence lines as data — sourced from boot-demo.html" },
    { path: "lib/game/story/act1.ts", change: "added", note: "Act 1 — Contact (~4 nodes)" },
    { path: "lib/game/story/act2.ts", change: "added", note: "Act 2 — The Hunt (~6 nodes)" },
    { path: "lib/game/story/act3.ts", change: "added", note: "Act 3 — Ascent (~5 nodes)" },
    { path: "lib/game/story/endings.ts", change: "added", note: "overrun, signal-lost, and rescued nodes" },
    { path: "lib/game/story/index.ts", change: "added", note: "findNode lookup + START_ID / OVERRUN_ID constants" },
    { path: "lib/game/index.ts", change: "added", note: "Barrel" },
    { path: "hooks/use-typewriter.ts", change: "added" },
    { path: "hooks/index.ts", change: "added", note: "Barrel" }
  ]}
/>

### Campaign content

<Table
  id="acts-table"
  columns={["Act", "Setting", "Beats", "Nodes"]}
  rows={[
    ["1 · Contact", "Crash site, first passages", "Establish the relay, teach HELP/STATUS/LOOK, first route choice, first hint of the creature", "~4"],
    ["2 · The Hunt", "Flooded galleries, the nest", "Creature is aware; hide/run/quiet choices drive the threat meter", "~6"],
    ["3 · Ascent", "Lava tubes toward the surface", "Route to open sky, flare signal, extraction — plus the shared overrun and ending nodes", "~5"]
  ]}
/>

Each act is one data file under `lib/game/story/`; if an act outgrows the 150-line cap (Act 2 is the likely one once `look` and `label` land on every node), it splits by chamber and the `story/` barrel keeps `findNode` oblivious. Dialogue is written lowercase-panicked for the crew and clipped-formal for ship systems — the two voices carry the tension since there are no visuals.

### Visual treatment

Two self-hosted Google fonts: **Press Start 2P** for the title and headers (`--font-pixel`), **VT323** for all terminal text (`--font-terminal`) — swapped into `app/layout.tsx` in place of the Geist pair. The CRT look lives entirely in `app/globals.css`, and the boot screen follows the late-80s/early-90s BIOS homage proven out in `boot-demo.html`: white VGA text on black, staged line reveals, a memory-test count-up, "Detecting…" probes that resolve after a beat, and the Deep Range Star badge.

<Code
  id="crt-css"
  filename="app/globals.css"
  language="css"
  caption="Planned additions — phosphor palette, scanline overlay, reduced-motion guards."
  code={":root {\n  --crt-bg: #030905;\n  --crt-ink: #49f770;\n  --crt-dim: #1d7a3a;\n  --crt-warn: #ffb347;\n  --crt-alert: #ff4f4f;\n  --crt-bios: #e4e4e4;\n}\n\n.crt::after {\n  content: '';\n  position: fixed;\n  inset: 0;\n  pointer-events: none;\n  background: repeating-linear-gradient(0deg, transparent 0 2px, rgb(0 0 0 / 18%) 2px 4px);\n}\n\n.crt-glow {\n  text-shadow: 0 0 6px color-mix(in srgb, var(--crt-ink) 55%, transparent);\n}\n\n@media (prefers-reduced-motion: reduce) {\n  .starfield span, .crt-flicker, .cursor-blink {\n    animation: none;\n  }\n}"}
/>

The boot phase renders in BIOS white (`--crt-bios`) for authenticity, then the terminal phase switches to phosphor green — the palette shift doubles as a story beat: the ship's general OS hands off to the comms relay.

Accessibility is designed in, not bolted on: the terminal log is an `aria-live="polite"` region, the input is labeled and keeps focus for the whole game, every screen advances by keyboard alone, and `prefers-reduced-motion` disables flicker, starfield drift, and cursor blink while keeping the typewriter skippable via Enter. Log lines render as text nodes — never `dangerouslySetInnerHTML` — so story content can never inject markup.

<Callout id="risk-legibility" tone="risk">

VT323 gets hard to read small and pure-green-on-black can fail contrast at low weights. Mitigation: minimum 18px terminal text, contrast-check `--crt-ink` against `--crt-bg` (target ≥ 7:1), and run the `ada-wcag-compliance` audit before shipping.

</Callout>

<Callout id="risk-typewriter" tone="risk">

A naive per-character `setState` across a long log can jank. Mitigation: only the newest line animates (older lines render as plain text), one interval at a time via `use-typewriter`, and Enter instantly reveals. If profiling still shows jank, batch reveals to 2–3 characters per tick.

</Callout>

### Build order

<Checklist
  id="build-order"
  items={[
    { id: "stage-theme", label: "Theme foundation — fonts in app/layout.tsx, CRT tokens and effects in app/globals.css", note: "First read the bundled Next docs (node_modules/next/dist/docs — fonts, CSS, metadata) per AGENTS.md; design-skill pass (design-taste router + frontend-design) happens here" },
    { id: "stage-shell", label: "Phase shell — game.tsx over gameReducer, title-screen, starfield" },
    { id: "stage-cinematics", label: "Cinematics — intro-crawl and boot-sequence (port boot-demo.html) on use-typewriter, skip handling" },
    { id: "stage-terminal", label: "Terminal UI — log, input, status bar, aria-live wiring" },
    { id: "stage-engine", label: "Engine — types, parser, engine, Act 1 content", note: "bun test: applyCommand paths incl. escaping the overrun node, ending flows, plus graph validation (every match.next / START_ID / OVERRUN_ID resolves; both endings reachable)" },
    { id: "stage-campaign", label: "Full campaign — Acts 2–3, endings.ts, threat tuning" },
    { id: "stage-gates", label: "Ship gates — ada-wcag audit, reduced-motion pass, code review, build + lint clean" }
  ]}
/>

### Verification

Beyond `npm run build`, `npm run lint`, and the `bun test` suite (engine paths + graph validation), the real check is a full keyboard-only playthrough on `npm run dev`:

1. Enter on the title → intro types out → Enter reveals, S skips → boot autoplays into the terminal.
2. Type `HELP`, then `STATUS`, then `LOOK`, then gibberish — expect the command list, the status line with the location label, the chamber description, and an in-fiction fallback that restates the current choice.
3. Play the golden path (level passage → quiet → hide → left → wait → flare) to the **CREW SECURED** ending with stats, then Enter — back to a fresh title.
4. Start again; deliberately pick noisy routes until threat hits 5 — expect the overrun node's last-ditch choice, then **SIGNAL LOST**; Enter returns to the title and a completely fresh run (reload mid-game also starts fresh — nothing persists).
5. Toggle `prefers-reduced-motion` in devtools — starfield, flicker, and cursor blink freeze; the game stays fully playable.

Review gates before commit: `ada-wcag-compliance` on the four screens, `/mrbme-code-review` on the diff.

### Open Questions

<QuestionForm
  id="open-questions"
  questions={[
    {
      id: "audio",
      title: "Audio for v1?",
      mode: "single",
      options: [
        { id: "silent", label: "Silent v1", detail: "Ship the game, add sound later. Keeps scope tight.", recommended: true },
        { id: "sfx", label: "8-bit SFX", detail: "WebAudio-generated typing blips and alert tones, mute toggle. No audio assets needed." },
        { id: "full", label: "Chiptune loop + SFX", detail: "Adds an audio asset and an autoplay-policy dance. Biggest scope add." }
      ]
    },
    {
      id: "notes",
      title: "Anything to change before the build starts?",
      mode: "freeform",
      placeholder: "Story tone, screen tweaks, scope cuts…"
    }
  ]}
  submitLabel="Send answers"
/>
